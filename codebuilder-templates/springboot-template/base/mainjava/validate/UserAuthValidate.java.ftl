package ${builderUtil.getSubPackage("validate")};

/**
 * 用户信息校验接口
 * 
 * @author ${baseInfo.author}
 *
 */
public interface UserAuthValidate {
  /**
   * 认证提交
   * 
   * @author ${baseInfo.author}
   *
   */
  interface Auth {
  }

  /**
   * 信息提交
   * 
   * @author ${baseInfo.author}
   *
   */
  interface Info {
  }

  /**
   * 密码提交
   * 
   * @author ${baseInfo.author}
   *
   */
  interface Password {
  }
}
